# Soda Demos

Example games and demos, run from the repo root:

```
./soda demos/whatever.ms
```

The `lib` symlink here points back at `../lib`, so these scripts can
`import "soda"` exactly the way a real game would.
